package com.powder.simplebeertime.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

object SimpleColors {

    // Background (ビール色：薄いゴールド)
    val Background = Color(0xFFFFF8E1)

    // Card (ビール色：琥珀)
    val Card = Color(0xFFFFE082)
    
    // Card Gradient（グラデーション用）
    val CardStart = Color(0xFFE6C86E)         // カード左側：濃いめ
    val CardEnd = Color(0xFFFFF0B0)           // カード右側：薄め

    // Graph Background (明るいクリーム)
    val GraphBackground = Color(0xFFFFFDE7)

    // Primary Button / Active Nav (ビール色：ダークアンバー)
    val ButtonPrimary = Color(0xFFFF8F00)
    
    // Button Gradient（グラデーション用）
    val ButtonStart = Color(0xFFE67A00)       // ボタン左：濃いめ
    val ButtonEnd = Color(0xFFFFB040)         // ボタン右：薄め

    // Text (タイトル・数値など)
    val TextPrimary = Color(0xFF2B2B2B)

    // Text (補足説明など)
    val TextSecondary = Color(0xFF4A4A4A)

    // Graph Label (グラフの数値)
    val GraphLabel = Color(0xFF000000)

    // Pure Colors (リンク・削除・グラフ棒など)
    val PureBlue = Color(0xFF0000FF)
    val PureRed = Color(0xFFFF0000)


    val DialogBackground = Color(0xFFFFF3CC)

    val SettingsDialogBackground = Color(0xFFFFE8A6)
}